// @flow
import React from 'react';
import { Box } from '../../common/components';

const Footer = () => (
  <Box
    borderTopWidth={1}
    borderStyle="solid"
    flexDirection="row"
    marginTop={1}
    paddingVertical={1}
  >
  </Box>
);

export default Footer;
